<div class="banner mb-5" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/background.png);">
  <div class="container">
    <h1>My milkshake brings all the attendees to the yard</h1>
    <p>Change your life and learn to code at one of our campuses around the world.</p>
    <% if !current_user %>
      <%= link_to 'Start now', new_user_session_path, class: "btn btn-flat" %>
    <% end %>
  </div>
</div>

<div class="container">
  <div class="row">
    <% Event.first(3).each do |event| %>
      <div class="home-cards col-sm">
        <%= render "events/event_card", event: event %>
      </div>
    <% end %>
  </div>
  <div class="l_events">
  <%= link_to 'See all Events', events_path  %>
  </div>
</div>
